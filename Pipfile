[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
torch = ">=2.5.0,<2.6.0"
transformers = ">=4.46.0"
datasets = ">=3.0.0"
peft = ">=0.13.0"
trl = ">=0.11.0"
pydantic-settings = ">=2.6.0"
click = ">=8.1.0"

[dev-packages]
pytest = ">=8.4.2"
pytest-cov = ">=7.0.0"
ipython = ">=9.6.0"
black = ">=25.9.0"
ruff = ">=0.13.3"

[requires]
python_version = "3.12"

